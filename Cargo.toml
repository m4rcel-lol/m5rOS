[workspace]
members = [
    "bootloader",
    "kernel",
]
resolver = "2"

[workspace.package]
version = "0.2.0"
authors = ["m5rOS Team"]
edition = "2021"
license = "MIT"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = 2
